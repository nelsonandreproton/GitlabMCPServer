# Logs (local dev artifacts — never deploy or commit these)
*.log

# Secrets
.env
*_tokens.json

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Usage stats (runtime data, lives on volume in prod)
usage-stats.json
